import * as Styles from './styles';

const Logo = () => {
  return (
    <Styles.LogoWrapper href="/">
      <Styles.LogoImage src="./logo.png" />
    </Styles.LogoWrapper>
  );
};

export default Logo;
